# Variables de entorno de ClubPlaza.
# Copiá este archivo a `.env` y completá los valores. El cliente real de Supabase
# (src/lib/supabase.ts) las usa cuando el backend esté conectado.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
